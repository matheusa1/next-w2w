import { ReactElement } from "react";

const ResultsWrapper = (): ReactElement => {
  return <div className="full grid w-full h-screen grid-cols-2 gap-2 bg-red-500"></div>;
};

export default ResultsWrapper;
